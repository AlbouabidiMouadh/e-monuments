import {StyleSheet} from 'react-native';
export const roundWhiteButtonStyle = StyleSheet.create({
  image: {
    width: 24,
    height: 24,
  },
  container: {
    borderRadius: 100,
    position: 'absolute',
    top: 120,
    right: 20,
  },
});
